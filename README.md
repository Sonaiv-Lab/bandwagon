## scripts

- `docker:build:piggyback`: build the docker image
- `docker:run-local:piggyback`: run the docker image only for local (so mount the .cert file). it would be better able to choose version in future